# Deployed Product Service (CDK API Gateway). No trailing slash.
# Example: https://abc123.execute-api.eu-central-1.amazonaws.com/prod
VITE_API_PRODUCT=

# Optional: single base for every service (overrides individual URLs when set)
# VITE_API_BASE=
